import React from "react";
import * as styles from "./rounded-input.module.css";

const RoundedInput = ({ className }) => {
  return (
    <div className={className}>
      <input />
    </div>
  );
};

export default RoundedInput;
